{
  "name": "ttpin",
  "private": true,
  "version": "0.1.0",
  "description": "ttPin - desktop translation widget with Azure LLM translation",
  "license": "PolyForm-Noncommercial-1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run -ws dev",
    "build": "npm run -ws build",
    "lint": "npm run -ws lint",
    "typecheck": "npm run -ws typecheck"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "eslint-plugin-react-hooks": "^7.0.1",
    "typescript-eslint": "^8.53.0"
  }
}
